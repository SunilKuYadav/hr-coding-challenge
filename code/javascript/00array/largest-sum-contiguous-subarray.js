const input = [-2, -3, 4, -1, -2, 1, 5, -3];

const sum = (arr) => {
    let finalMax = Number.MAX_SAFE_INTEGER, // track final max
        currentMax = 0, // track current max
        startIndex = 0, // track current max start index
        endIndex = 0; // track current max end index

    // loop over arr
    arr.forEach(element => {

    });


}

const out = sum(input);
console.log("Input : ", input);
console.log("Max sum : ", out)